# 待辦事項

## 並行且不阻塞 MVP：Vditor `ir` 行動版原型

保底方案是 Markdown 原始碼編輯器；本原型不得成為其他 MVP 功能開始或完成的前置條件。

### 通過條件

- 在手機瀏覽器輸入 `*`、`-`、`#`、連結等支援語法時可即時渲染，游標進入相關內容後可直接調整原始語法。
- 虛擬鍵盤開啟時，必要的格式工具列仍固定可見且不遮擋目前輸入位置。
- 注音等繁體中文輸入法不出現重複字、漏字、組字提前提交或游標跳動。
- 支援的 Markdown 經載入、編輯、輸出後語意與內容保持一致；不支援的語法不得靜默遺失。
- 可串接既定的自動儲存、空白新筆記、待確認移除與軟刪除流程，且失敗狀態仍可見。
- 在真實手機完成新增、編輯、切換畫面、斷線失敗及重新載入測試。

### 裁決

- 全部通過：可在 MVP 內以 Vditor `ir` 取代保底編輯器。
- 任一資料遺失、中文輸入或手機工具列硬性條件未通過：不阻塞 MVP，維持保底編輯器，原型移至 MVP 後處理。

## P0（MVP 完成後）：QNAP 異地備份與還原

這是 MVP 完成後的最高優先項目，不屬於 MVP 驗收範圍。完成前，Supabase PostgreSQL 與 Storage 沒有 QNAP 異地復原保障。

### 驗收條件

- 由 QNAP 排程匯出 Supabase PostgreSQL logical dump。
- 同步全部 Supabase Storage 原檔，不只備份 `storage` schema 中的中繼資料。
- 產生 manifest，至少包含物件路徑、原始檔名、大小、雜湊與資料列關聯。
- 明確決定備份頻率、保留期限、失敗通知與憑證保存方式。
- 實際建立全新 Supabase 專案，完成 schema／資料還原、Storage 回傳及 manifest 數量與雜湊核對。
- 將完整操作寫成可重複執行的備份、還原及故障處理文件。
- 只有上述還原驗證通過後，才新增資源回收區的永久刪除功能；永久刪除須再次明確確認，並一併移除資料與 Storage 原檔。

### 已知風險

- Supabase Free 沒有可依賴的自動資料庫備份。
- Supabase Storage 沒有物件版本管理。
- 此項目完成前，供應商專案誤刪、資料庫操作錯誤或 Storage 永久刪除可能無法復原。

## MVP 後：更換中繼資料來源連結

MVP 只支援手動最小條目第一次連結來源。已連結條目若選錯結果，MVP 的處理方式是移入資源回收區後重新建立。

### 驗收條件

- 只能在同一遊戲類型及同一供應商內搜尋並選擇新的來源結果。
- 執行前明確顯示即將替換的來源資料並再次確認。
- 替換來源中繼資料、來源分類、系統連結與來源封面。
- 保留自訂名稱、自訂封面、平台、自由標籤、筆記、相簿、附件與自訂連結。
- 任何抓取或寫入失敗都不得留下新舊來源混合狀態；原來源須保持可用。

## MVP 後：依貢獻者探索收藏庫外遊戲

MVP 只保證點擊貢獻者後查看及篩選收藏庫內遊戲。BoardGameGeek XML API 未提供完整且明確的「依人物反查全部遊戲」介面；IGDB 可查參與公司，但不提供逐遊戲的個人製作名單，因此不能以現有兩個來源承諾一致的庫外探索結果。

### 驗收條件

- 重新調查可合法使用的個人製作名單來源、反向查詢 API 或可維護索引，記錄申請資格、費用、速率限制與保存條款。
- 桌遊至少涵蓋設計與美術；電子遊戲至少涵蓋設計、創意領導與美術，且不得把公司負責人推定為每款遊戲的製作者。
- 每位貢獻者及每款庫外遊戲都有穩定來源身分，可與本地貢獻者及遊戲條目去重。
- 點擊貢獻者後清楚區分「我的遊戲」與「來源結果」，來源失敗不得影響收藏庫內結果。
- 將庫外結果加入清單時只建立遊戲引用；正式加入收藏庫後須保留既有清單關係。

## MVP 後：編輯遊戲結構化人數

MVP 只顯示 BoardGameGeek 或 IGDB 帶入的一般多人範圍與單人模式，不提供修改。若實際使用證明來源值經常需要整理，再加入來源值與擁有者覆寫值分離的編輯能力。

### 驗收條件

- 來源值與擁有者覆寫值分開保存；重新整理中繼資料不得覆寫擁有者值。
- 介面可清除覆寫並恢復顯示最新來源值。
- 驗證一般多人範圍必須成對存在且最小值不大於最大值；單人模式維持支援、不支援或未知三態。

## MVP 後：合併重複貢獻者

MVP 只依「供應商＋來源 ID」自動去重；同名但來源身分不同的貢獻者可分開存在。若實際資料出現跨來源重複，再提供人工確認的合併功能。

### 驗收條件

- 合併前顯示兩側的名稱、人物／組織類型、來源身分及全部遊戲關係。
- 合併後保留所有來源身分、貢獻分類及遊戲關係，不得因重複關係造成顯示重複。
- 同名不得自動合併，且不同名的別名或改名情況亦可由擁有者手動確認。
- 合併失敗時維持原本兩個貢獻者及其關係，不留下部分搬移狀態。

## MVP 後：保存 BGG 指標歷史

MVP 只保存最新的重度（Avg. Game Weight）、Strategy Game Rank 與最後成功更新時間。若實際使用需要觀察長期變化，再建立快照歷史與趨勢介面。

### 驗收條件

- 歷史快照包含遊戲、指標值、來源與擷取時間，且同次更新可識別為同一批次。
- 排名為未排名或指標缺漏時以明確狀態保存，不以 0 冒充。
- 日常更新失敗不新增不完整快照，也不影響最新成功值。
- 趨勢介面明確區分 Strategy Game Rank 與 Avg. Game Weight，不把兩者合成單一分數。
