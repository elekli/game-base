# 遊戲資料庫領域脈絡

## 目標

建立一套以繁體中文操作的個人遊戲資料庫，涵蓋桌遊、主機遊戲與電腦遊戲；正式服務部署於 Vercel，並在 MVP 後優先加入 QNAP 可還原備份。

## 詞彙

### 遊戲條目

代表一個可辨識的遊戲標題，是 MVP 的主要資料單位。封面、中繼資料、筆記、標籤、圖片、檔案與外部連結皆附屬於遊戲條目。

遊戲條目由使用者搜尋外部資料來源並選擇正確結果後建立；系統自動帶入封面與中繼資料，使用者不必逐欄輸入。MVP 不要求從 Steam、PlayStation、Xbox 或 BoardGameGeek 帳號自動同步收藏。

建立遊戲條目時，擁有者先選擇「桌遊」或「電子遊戲」，再搜尋該類型唯一對應的來源：桌遊只搜尋 BoardGameGeek，電子遊戲只搜尋 IGDB。MVP 不把兩個來源的候選結果混在同一份清單。

點擊外部搜尋結果後先顯示精簡確認畫面，確認前不建立資料。畫面至少包含封面、名稱、年份與來源；桌遊另顯示人數及設計師，電子遊戲另顯示來源支援平台及開發公司。只有擁有者明確按下「加入收藏庫」後才建立，且送出時再次以「供應商＋來源 ID」檢查重複。

確認畫面先取得該來源遊戲的完整資料。擁有者按下「加入收藏庫」時，伺服器須以同一「供應商＋來源 ID」重新取得並正規化資料。確認版本與建立版本以畫面上的遊戲辨識欄位比較：共同比較名稱與年份；桌遊另比較設計師與出版社；電子遊戲另比較來源支援平台、開發公司與發行公司。比較集合時不考慮排列順序，但來源 ID 或顯示名稱改變都算內容改變；封面、人數及其他未列欄位不參與比較。若辨識欄位相同，系統以建立時取得的完整最新快照建立；若已改變，系統更新確認畫面並要求再次確認，不得靜默寫入擁有者未確認的新辨識內容；若來源暫時失敗，保留確認畫面並提供重試或改建手動最小條目，不得以舊快照建立來源條目。

建立成功後直接進入新遊戲的詳細頁。手機版將「上傳照片」與「新增筆記」放在最容易觸及的主要操作區，平台、自由標籤、清單與資料編輯位於次要操作區。系統不得自動開啟筆記編輯器，以免妨礙立即上傳照片。

若外部資料來源找不到遊戲或暫時無法使用，擁有者可在已選定遊戲類型後，只填名稱建立最小條目。最小條目日後仍可從該類型對應的來源重新搜尋並連結，補入中繼資料；來源失敗不得阻止新增遊戲。

從 BoardGameGeek 搜尋結果建立的條目，其類型固定為桌遊；從 IGDB 建立的條目固定為電子遊戲。手動建立最小條目時，送出前須再次顯示並確認所選類型。

遊戲條目建立後，MVP 不允許直接修改遊戲類型，也不搬移筆記、圖片、附件或其他資料到另一類型。若類型選錯，擁有者將舊條目移入資源回收區後重新建立。

同一遊戲標題即使在多個平台遊玩，仍只建立一個遊戲條目；平台可複選。例如，同一個《Hades》條目可同時標示 Steam 與 PS5。MVP 不把不同平台版本或實體副本建模為獨立條目。

可篩選的「平台」只代表擁有者實際使用該遊戲的平台，由擁有者選擇及維護。IGDB 提供的支援平台清單屬於外部中繼資料，可供建立或編輯時參考，但不得自動寫入可篩選的平台欄位；未實際使用的平台不得使遊戲出現在該平台的篩選結果中。

電子遊戲的平台為選填欄位，不得阻礙「搜尋、選擇、建立」流程；建立後可隨時補充或修改。桌遊不顯示平台欄位。這允許擁有者先建立尚未遊玩、尚未購買或尚未決定遊玩平台的電子遊戲條目。

平台選擇器提供 Steam、PS5、Xbox Series、Nintendo Switch 等常用預設值，並允許擁有者新增自訂平台，以涵蓋舊主機與小眾平台。平台名稱建立前須移除前後空白，且英文字母比對不分大小寫；例如 `PS5` 與 `ps5` 是同一平台。介面保留該平台第一次建立時的顯示名稱。

系統預設平台不可改名或刪除；自訂平台可全域改名。仍被任一遊戲使用的自訂平台不得刪除，介面須顯示使用數量並可查看相關遊戲；只有移除全部遊戲關係後，才能再次確認並刪除空的自訂平台。

外部來源身分由「供應商＋來源 ID」組成，且在整個資料庫中唯一，包含已移入資源回收區的條目。資料庫必須以唯一約束守住此規則，不能只依賴搜尋畫面的前端檢查。

建立或連結來源時若身分已存在：一般條目直接開啟既有資料，不建立第二筆；資源回收項目則提示擁有者還原。並行請求、重複點擊或失敗重試也不得繞過唯一約束。

手動建立的最小條目沒有可靠的外部來源身分，因此名稱相同或近似只能視為「可能重複」，不能作為阻止建立的唯一依據。送出前，系統搜尋一般條目與資源回收項目的顯示名稱、原文名稱及其他別名，列出可能重複項目；擁有者檢視後仍可繼續建立。若可能重複項目位於資源回收區，介面同時提供還原選項。

### 外部遊戲身分

由供應商與來源 ID 組成的外部遊戲識別資料。它可先被庫外清單引用，並在同一來源遊戲加入收藏庫後連到正式遊戲條目；兩種用途不複製來源身分。不同供應商或不同來源 ID 一律是不同外部遊戲身分，即使名稱相同或彼此是改編、移植關係。

例如 BoardGameGeek 的《Barrage》與 IGDB 的 Steam《Barrage》是不同外部遊戲身分及不同遊戲條目；可透過關聯遊戲清單呈現兩者關係，但不得以同名自動合併。

### 遊戲名稱

一個遊戲條目可保存自訂顯示名稱、資料來源提供的原文名稱與其他別名。資料來源有繁體中文名稱時優先作為初始顯示名稱，否則使用原文名稱；擁有者可另行修改顯示名稱。

依標題搜尋時，同時比對自訂顯示名稱、原文名稱與其他別名。修改顯示名稱不得覆寫或遺失來源名稱。

MVP 的標題搜尋採即時的部分字串比對，不承諾錯字推測或近似字詞排序。英文搜尋不分大小寫；例如輸入 `zelda` 可找到名稱含有 `Zelda` 的條目，但輸入 `zelad` 不保證找到結果。搜尋行為應保持可預期，模糊搜尋延至資料量或實際使用需求證明有必要時再評估。

收藏庫的一般標題搜尋只查本地資料，不呼叫 BoardGameGeek 或 IGDB。外部搜尋只存在於「新增遊戲」流程內，並在擁有者先選定桌遊或電子遊戲後呼叫該類型對應的單一來源。

### 中繼資料來源

MVP 以 BoardGameGeek 作為桌遊的主要中繼資料來源，以 IGDB 作為電子遊戲的主要中繼資料來源。部署前需分別申請非商業 API 應用程式與憑證；憑證只由伺服器端保管及使用，不傳送到瀏覽器。

系統在建立遊戲條目時抓取中繼資料，一般中繼資料之後只在擁有者主動執行「重新整理中繼資料」時更新，不做定期背景同步；每日更新的唯一例外是另行定義的 BGG 重度與 Strategy Game Rank。重新整理不得覆寫自訂顯示名稱、自訂封面、平台、自由標籤、筆記、相簿圖片、遊戲附件或自訂連結。

擁有者主動重新整理中繼資料時，系統取得並驗證完整的最新來源快照後，直接以單一資料庫交易更新所有由來源管理的欄位與關係，不另顯示差異確認。有效快照中已消失的舊來源欄位或來源關係須一併移除；手動貢獻關係及其他擁有者資料不受影響。取得、正規化或寫入任一步驟失敗時，整次重新整理失敗並保留上一份成功快照；不得留下新舊混合的來源資料，也不得顯示為更新成功。

未連結來源的手動最小條目，可在 MVP 內第一次搜尋並連結該類型對應的來源。已經連結來源的條目則不能在 MVP 更換來源結果；若選錯，擁有者須將舊條目移入資源回收區後重新建立。

手動最小條目第一次連結來源時，若所選「供應商＋來源 ID」已屬於另一個一般或資源回收項目，系統必須阻止連結並開啟既有項目或提示還原。MVP 不自動合併兩個條目的筆記、圖片、附件、標籤或其他資料；原手動條目保持不變，由擁有者自行決定是否移入資源回收區。

同一類型及同一供應商內的「更換來源連結」延至 MVP 後。後續實作時，更換只替換來源中繼資料、來源分類、系統連結及來源封面，必須保留自訂名稱、自訂封面、平台、自由標籤、筆記、相簿、附件與自訂連結。

匯入的中繼資料與來源封面須永久保存於 Supabase PostgreSQL 與私有 Supabase Storage。外部中繼資料來源無法使用時，既有遊戲條目仍可查看與編輯；失效只影響搜尋外部資料、建立來源條目及重新整理中繼資料，且錯誤必須明確顯示。

在單人、非商業且受存取控制保護的既定範圍內，MVP 不區分來源封面與使用者封面的保存期限，兩者皆保存於私有 Supabase Storage bucket。QNAP 異地備份延至 MVP 後。這是擁有者明確接受的條款與可復原性風險；若用途改為公開服務、商業用途或對外散布，必須在擴大範圍前重新審查來源條款。

### 遊戲類型

遊戲的主要媒介分類。MVP 至少包含「桌遊」與「電子遊戲」，一個遊戲條目只有一種遊戲類型，且建立後不可修改。

### 平台

遊玩電子遊戲所使用的平台，例如 Steam、PS5 或 Xbox。一個遊戲條目可對應多個平台；桌遊可不設定平台。

### 自由標籤

使用者自行建立、可複選的補充分類，例如「合作」、「派對」或「劇情向」。自由標籤不取代遊戲類型與平台。

自由標籤是整個資料庫共用的分類項目，不是複製到各遊戲內的獨立文字。同一標籤可附加到多個遊戲；標籤改名時，所有關聯遊戲與篩選結果同步反映新名稱。標籤名稱建立或改名時須移除前後空白，且英文字母比對不分大小寫，以避免建立名稱等價的重複標籤。

仍被任一遊戲使用的自由標籤不得刪除，介面須顯示使用數量並可查看相關遊戲；只有移除全部遊戲關係後，才能再次確認並刪除空標籤。MVP 不建立平台或自由標籤的資源回收區。

### 來源分類

由 BoardGameGeek 或 IGDB 中繼資料帶入的類型、題材或遊戲機制。來源分類與自由標籤分開保存，兩者皆可篩選。

每個來源分類保留供應商、來源內的分類種類、來源 ID 與名稱，不能只保存一段無種類的文字。例如 BoardGameGeek 的「機制：合作遊戲」與「類別：奇幻」，以及 IGDB 的「類型：角色扮演」與「題材：奇幻」，即使名稱相同也仍是不同分類。介面統稱「來源分類」，但依類型、題材、機制等來源種類分組顯示。

分類種類只能由來源 API 的明確欄位判定，不得依名稱或文字內容猜測。MVP 採固定對應白名單：BoardGameGeek 的 `boardgamecategory` 對應「類別」、`boardgamemechanic` 對應「機制」；IGDB 的 `genres` 對應「類型」、`themes` 對應「題材」、`game_modes` 對應「遊戲模式」、`player_perspectives` 對應「玩家視角」。IGDB 的 `keywords` 與彙整式 `tags` 暫不匯入來源分類，以免把大量未分級關鍵字混入主要篩選器。來源新增或回傳未知種類時，MVP 不顯示為可篩選分類，須先新增明確對應後才能納入。

手動重新整理中繼資料時可以更新來源分類，但外部來源永遠不得新增、改名或刪除擁有者的自由標籤。

來源分類若沒有繁體中文資料，MVP 直接顯示來源原文，不做自動翻譯。繁體中文要求適用於介面、操作訊息及擁有者自訂內容，不保證外部中繼資料皆有繁中版本。

### 遊戲人數

桌遊與電子遊戲共用人數模型，將同地多人範圍與單人模式分開保存。同地多人模式要求所有玩家處於同一空間；桌遊包含同桌遊玩，電子遊戲包含同機、分割畫面及 Switch 等多機在場連線，但不包含純線上連線或造訪他人遊戲世界。多人範圍由可為空的 `group_player_min` 與 `group_player_max` 表示；兩者必須同時存在或同時留空，且最小值不得大於最大值。單人模式由 `solo_support` 表示，值為「支援」、「不支援」或「未知」。另有一段選填的 `player_count_note`，只補充特殊限制，不取代結構化欄位。

例如「2～4 人，可單人遊玩」保存為一般多人範圍 2～4、單人模式為支援；純單人遊戲可將一般多人範圍留空並標示支援單人。MVP 的結構化人數值只由中繼資料來源帶入並以唯讀方式顯示，不提供擁有者修正或來源覆寫介面；未連結來源的手動條目顯示為未知。選填的人數說明仍可由擁有者補充，但不改變結構化值。

### BoardGameGeek 動態指標

「重度（BGG Avg. Game Weight）」是本產品使用者目前最重視的 BGG 指標，Strategy Game Rank 則是策略類桌遊的重要排名；兩者都屬於 MVP 必要中繼資料。策略排名保存為可為空的正整數；未進入策略排名時顯示「未排名」。重度保存為可為空的 1～5 小數，並明確標示為 BGG 社群平均重度，而非客觀難度。

兩項指標均只保存最新快照與最後成功更新時間，MVP 不保存每日歷史或提供趨勢圖。建立來源遊戲時立即抓取；之後在每個台北日第一次通過身分驗證進入應用程式時，背景更新已過期的 BGG 指標，且同一天最多啟動一次。手動重新整理某遊戲的中繼資料時亦更新該遊戲指標。一般列表與遊戲頁先顯示快取值，不等待或即時請求；更新失敗時保留舊值及其時間，不得清空或阻擋其他功能。

桌遊卡片直接顯示重度；遊戲詳細頁同時顯示重度與 Strategy Game Rank。桌遊篩選器可設定最低與最高重度，排序可選重度由輕到重、由重到輕，以及 Strategy Game Rank 由前到後；缺少所選排序值的遊戲排在有值者之後。

每日背景流程只處理一般收藏庫內且已連結 BoardGameGeek 的遊戲，並且只寫入重度、Strategy Game Rank 與最後成功更新時間。資源回收項目、庫外清單引用及其他中繼資料不參與每日更新；手動執行「重新整理中繼資料」時才更新該遊戲的完整來源資料。

應用程式選單提供不阻擋操作的背景更新狀態，至少顯示完成數／總數、最後成功時間與失敗筆數。失敗保留舊資料並允許只重試失敗項目，不逐筆彈出通知，也不得重複啟動同一日的並行更新批次。

### 自動中繼資料範圍

MVP 共通匯入名稱、別名、封面、來源介紹與最早發行年份。桌遊與電子遊戲皆匯入可取得的同地多人範圍與單人支援。

「來源介紹」指 BoardGameGeek 的遊戲 description 或 IGDB 的 summary，不是擁有者筆記、評論、規則書或遊玩紀錄。來源介紹依來源原文唯讀保存，重新整理完整中繼資料時可更新；顯示前須安全清理，不得執行來源 HTML。遊戲詳細頁預設收合長篇來源介紹，使機制、美術、貢獻者與重度等主要資料保持優先。手動最小條目沒有來源介紹時不要求填寫，也不另設自訂介紹欄位；個人補充內容使用筆記。

桌遊另匯入遊戲時間、類別、機制、設計／開發、美術、發行、重度（BGG Avg. Game Weight）與 Strategy Game Rank。電子遊戲另匯入來源支援平台、類型、題材、遊戲模式、玩家視角、開發公司與發行公司；來源缺少的個人設計與美術可由擁有者手動補充。

MVP 不匯入建議年齡、一般評分、整體排名、熱門度、來源截圖、來源影片或純線上遊玩人數。Strategy Game Rank 是明確例外，依上述動態指標規則匯入及更新。

### 貢獻者

對遊戲的設計、視覺印象與發行有辨識價值的人或組織。MVP 將所有貢獻關係收斂為「設計／開發」、「美術」與「發行」三個固定分類，不建立更細的職稱或可自訂角色系統。三個分類都是遊戲條目的核心中繼資料，不得只以無結構的介紹文字保存。

貢獻者是全庫共用的「人物」或「組織」實體；遊戲透過帶有固定分類與資料來源的貢獻關係連到貢獻者，而不是把姓名複製成普通標籤。同一貢獻者可出現在同一遊戲的多個分類。BoardGameGeek 的設計師、插畫／美術人員與出版商分別映射到三個分類；IGDB 的開發公司映射到「設計／開發」，發行公司映射到「發行」，而「美術」通常留空並允許擁有者手動補充。系統不得把工作室負責人自動視為該工作室每款遊戲的設計者。

來源匯入的貢獻關係在 MVP 內唯讀，介面標示其來源；擁有者可在三個固定分類中另外新增或移除手動貢獻關係。重新整理中繼資料只更新來源關係，不得修改手動關係。MVP 不提供編輯、移除或在本地隱藏單一來源貢獻關係的功能。

手動新增貢獻者時只要求名稱及「人物／組織」類型；所屬貢獻分類由目前操作的「設計／開發」、「美術」或「發行」欄位決定。MVP 不要求介紹、細部職稱或外部連結。同名只提示可能重複，擁有者仍可建立。

自動匯入時，只有相同「供應商＋來源 ID」才視為同一貢獻者。名稱相同或經正規化後相同只能提示可能重複，不得自動合併，因為不同人物或組織可能同名。MVP 允許可能重複的貢獻者暫時分開存在，不提供合併工具。

點擊貢獻者可查看收藏庫內與其相關的遊戲，亦可把貢獻者作為篩選條件。同一貢獻分類內選取多位貢獻者時取聯集，不同分類之間取交集。依貢獻者探索收藏庫外遊戲不屬於 MVP，須待找到能涵蓋個人製作名單及反向查詢的合法資料來源或索引方案後再加入。

MVP 不匯入或顯示桌遊建議年齡。

### 篩選

同一維度內的多選條件取聯集，不同維度之間取交集。每一種來源分類亦視為獨立維度：同種類內取聯集，不同種類間取交集。例如選取「機制：合作或牌庫構築」再選「題材：奇幻」，結果須符合其中一種機制，並且具有奇幻題材。

標題搜尋可與遊戲類型、平台、來源分類及自由標籤篩選同時使用。

來源分類篩選器只有在擁有者先選定單一遊戲類型後才顯示：桌遊顯示 BoardGameGeek 的類別與機制，電子遊戲顯示 IGDB 的類型、題材、遊戲模式與玩家視角。未選遊戲類型或同時選取多種類型時，不顯示來源分類篩選器，也不保留先前選取的來源分類條件。自由標籤不受此限制，可跨桌遊與電子遊戲篩選。

### 封面

遊戲條目在列表與詳細頁的主要識別圖片。建立條目時預設採用外部資料來源提供的封面；擁有者可另行上傳自訂封面，或從該遊戲的相簿指定圖片取代。系統保留原始封面網址，以便恢復自動封面。

封面與相簿中的遊玩相片／截圖用途不同，兩者不共用同一欄位。

### 相簿圖片

附屬於遊戲條目的相片或截圖。每張圖片自動記錄上傳時間，並可附一段選填說明文字。

圖片說明是上傳後可補充的附加資訊；留白不得阻擋或延後圖片上傳。使用者可先完成上傳，再視需要新增或修改說明。

手機端可一次選取並上傳多張圖片。批次上傳先建立所有圖片，個別說明與指定封面等整理動作在上傳完成後進行。

批次中的每張圖片獨立上傳。單一檔案超過限制或上傳失敗時，其他有效檔案仍繼續；介面逐檔顯示等待中、上傳中、成功或失敗狀態與繁體中文原因，並允許只重試失敗項目。已成功的圖片不得因重試失敗項目而重複建立。

每個遊戲條目只有一個相簿，圖片依上傳時間排列。MVP 不提供子相簿或手動排序。

### 筆記

附屬於遊戲條目的獨立文字內容。一個遊戲條目可有多則筆記，每則自動記錄建立與修改時間，並可個別編輯或移除。

筆記用於持續追加不同時間留下的心得，但不要求對應某次特定遊玩，也不包含遊玩日期、玩家、局數等遊玩紀錄欄位。

筆記內容以 Markdown 純文字字串保存，Markdown 是權威資料格式。閱讀時將 Markdown 渲染為格式化內容；原始 HTML 不直接執行，須停用或經安全清理後才可顯示。

MVP 的保底編輯體驗採 Markdown 原始碼編輯器：`*`、`-`、`#` 等語法標記在編輯時保持可見，使用者可直接輸入，也可用工具列插入或包住選取文字。工具列不把內容轉成 HTML 或專屬富文字格式。

Vditor `ir` 行動版原型與主專案並行開發，但不得阻塞其他 MVP 功能。若原型通過語法揭露、固定行動工具列、繁體中文輸入法、Markdown 往返及既有自動儲存狀態的驗證，則可在 MVP 內取代原始碼編輯器；若未通過，MVP 仍以保底編輯器交付。

筆記新增與編輯採自動儲存。使用者停止輸入後系統送出變更，介面持續顯示「儲存中」、「已儲存」或「儲存失敗」；儲存失敗時文字保留在編輯器內並可重試，不得回復舊內容或顯示成已儲存。

使用者在儲存尚未完成時離開頁面，系統須先嘗試完成儲存；若無法完成，必須提示仍有未儲存內容。MVP 不把失敗內容加入離線佇列。

新建筆記若沒有輸入內容或只有空白，不建立資料，也不顯示為一則既有筆記；判斷空白時可忽略前後空白，但非空筆記仍保留使用者實際輸入的文字。

既有筆記被清空或只剩空白時，不把空內容自動儲存到伺服器，而是進入「待確認移除」狀態，伺服器繼續保留清空前的內容。若使用者繼續輸入非空白內容，恢復一般自動儲存。

使用者從應用程式內離開待確認移除的筆記時，系統詢問是否移除筆記；選擇移除才執行刪除，選擇保留則還原清空前內容。若尚未詢問就斷線或直接關閉分頁，伺服器不得變更，重新開啟時顯示清空前內容。MVP 不保證在瀏覽器關閉分頁時顯示自訂確認視窗。

筆記移除採軟刪除：資料列與文字內容持續保留，但不顯示在一般遊戲頁面。移除後介面提供立即復原操作；MVP 不建立獨立的筆記資源回收頁，也不永久清除筆記。清空既有筆記並確認移除時，同樣走軟刪除流程。

### 遊戲附件

附屬於遊戲條目的一般檔案，例如 PDF 規則書或玩家輔助表。每個附件保留原始檔名，並可設定顯示名稱與選填說明。

MVP 支援附件上傳、開啟或下載，但不提供版本管理、文件註記或專用閱讀器。

### 上傳限制

自訂封面、相簿圖片與遊戲附件的單檔上限統一為 50 MB。瀏覽器須在開始上傳前檢查檔案大小，超過時立即拒絕並顯示繁體中文錯誤；伺服器端仍須再次驗證，不能信任前端檢查。

MVP 不自動壓縮、拆分或修改超過限制的檔案。若真實規則書或其他必要附件無法符合限制，再重新評估 Supabase 方案或物件儲存供應商。

### 圖片原檔與縮圖

相簿圖片與自訂封面永久保存使用者上傳的原檔，不做有損壓縮、縮小或格式轉換。原檔是權威資產，不得以縮圖取代。

系統另產生顯示用縮圖，供遊戲列表、搜尋結果及一般相簿瀏覽使用，以降低頻寬並加快載入。縮圖是可由原檔重新產生的衍生物；縮圖遺失或損壞不得影響原檔，刪除或還原圖片時則須同步處理其縮圖。

MVP 為每張相簿圖片、自訂封面與永久保存的來源封面預先產生一份固定規格的 WebP 縮圖，並存入同一個私有 Supabase Storage。MVP 不依賴 Supabase 即時圖片轉換，也不建立多組響應式尺寸。

原檔上傳成功後，即使縮圖產生失敗也不得回滾或刪除原檔。系統須記錄縮圖狀態、顯示繁體中文錯誤並允許重試；一般瀏覽在縮圖不可用時可暫時顯示原檔，但不得靜默隱藏圖片。

遊戲列表、搜尋結果與相簿網格只載入縮圖，不預先載入原檔。只有擁有者點開單張圖片或明確下載時，系統才核發短效網址並載入原檔；點開檢視時仍須提供下載原檔的操作。

### 外部連結

附屬於遊戲條目的「名稱＋網址」，並區分為系統連結與自訂連結。系統在建立條目時可自動加入中繼資料來源頁面，例如 BoardGameGeek 或 IGDB；擁有者也可自行新增其他連結。

重新整理中繼資料時可更新系統連結，但不得修改或刪除自訂連結。

### 來源資料

由 BoardGameGeek 或 IGDB 取得、附屬於外部遊戲身分的白名單中繼資料。外部遊戲身分可尚未連到遊戲條目，供庫外清單引用使用；來源資料保留供應商、來源 ID、原始頁面連結與最後成功同步時間。

來源資料與擁有者輸入的顯示名稱、自由標籤、筆記、自訂連結及自訂封面分開保存。重新整理只能替換來源資料，不得覆寫擁有者資料；手動建立的最小條目可尚未連結外部遊戲身分。

### 遊戲清單

由擁有者維護的一組遊戲引用。MVP 支援一般命名清單，以及附屬於某個遊戲的特殊「關聯遊戲」清單；後者表達「與遊戲 X 相關的一組遊戲」。MVP 不提供清單共享、巢狀清單或複雜排序規則。

一般清單名稱須移除前後空白，且英文字母比對不分大小寫；名稱在一般與封存清單中都必須唯一。建立時若命中封存清單，介面不得建立同名新清單。封存清單含有庫內遊戲時，介面提供一個成員遊戲入口，讓擁有者到該遊戲的「相關封存清單」區段還原；若清單只有庫外引用而沒有庫內遊戲，建立畫面直接顯示封存清單名稱、成員數與還原按鈕。MVP 仍不建立全域封存清單頁。

新增一般清單時，只有名稱且尚未加入任何成員的清單視為未持久化草稿，不先寫入資料庫。可偵測使用者離開清單建立畫面時，介面提示空清單將被放棄；若直接關閉分頁、斷線或其他情況無法提示，草稿自然消失，不留下待清理的空清單。加入第一個成員後才建立正式清單。

MVP 不提供清單項目的手動排序；一般清單與關聯清單都依遊戲顯示名稱排列。每個清單項目可附一段選填短說明，用來記錄該遊戲與此清單的關係；說明屬於清單項目，不是遊戲的一般筆記。加入遊戲時不得要求先填說明，建立後可再補充或修改。

「關聯遊戲」是對稱關係。將遊戲 Y 加入遊戲 X 的關聯清單後，Y 亦顯示 X；資料庫只保存一份無方向關係，從任一側移除都會解除雙方關聯。具有方向性的語意，例如「受到這款遊戲啟發」，使用一般命名清單表達，不套用對稱規則。

清單項目可指向收藏庫內的遊戲條目，也可保存收藏庫外的遊戲引用。庫外引用保存供應商、來源 ID、名稱、年份及一份存於私有 Supabase Storage 的本地 WebP 封面縮圖，但不保存封面原檔或完整中繼資料；加入清單不等於建立遊戲條目。若日後以相同「供應商＋來源 ID」正式建立遊戲條目，所有既有清單項目自動改連到該條目，且不得遺失清單成員關係；此時才依一般遊戲條目規則保存來源封面原檔。

庫外遊戲引用只能由 BoardGameGeek 或 IGDB 搜尋結果建立，必須具有「供應商＋來源 ID」。來源找不到遊戲時，擁有者須先建立手動最小遊戲條目，再把該庫內條目加入清單；MVP 不建立只有名稱而沒有穩定來源身分的庫外引用。

遊戲條目移入資源回收區時，所有清單成員關係與對稱的關聯遊戲關係都持續保留。清單內以灰階項目及「已移入資源回收區」標記顯示，並提供還原操作，不得靜默移除；還原後自動恢復一般顯示。

刪除一般清單時採軟刪除並稱為封存，完整保留清單名稱、成員與項目說明。MVP 不建立全域「封存清單」頁；封存清單會出現在其每個庫內成員的遊戲頁「相關封存清單」區段，並可從該處還原。只有庫外引用的封存清單則在建立同名清單時顯示並直接還原。從一般清單移除單一遊戲時軟刪除該成員關係並提供立即復原；解除對稱的關聯遊戲時亦提供立即復原。

### 遊玩紀錄

代表某一次實際遊玩的事件。MVP 不建立獨立遊玩紀錄；若未來需要日期、參與者、局數或逐場相片，再另行擴充。

### 資源回收項目

被擁有者刪除、但尚未永久移除的遊戲條目。移入資源回收區時，條目的中繼資料、筆記、標籤、圖片、附件與連結都完整保留，並可原樣還原。

移入資源回收區前顯示確認畫面，列出將一併從一般介面隱藏但仍保留的筆記、照片、附件、清單與關聯數量；不要求輸入遊戲名稱。完成後提供立即復原，資料庫內容與 Supabase Storage 檔案都不得刪除或改寫。

資源回收項目不出現在一般搜尋與篩選結果中。MVP 不自動清空資源回收區，也不提供永久刪除；擁有者只能將項目移入資源回收區或原樣還原。

永久刪除延至 QNAP 異地備份及實際還原驗證完成後再加入，避免在沒有復原途徑時移除資料與 Storage 原檔。

### 擁有者

使用及管理整個遊戲資料庫的唯一使用者。MVP 只有一位擁有者，不處理多人帳號、資料歸屬、分享或分級權限。

## MVP 邊界

- 以遊戲條目為核心，不建立逐次遊玩的資料模型。
- 同一遊戲標題只建立一個條目，平台可複選。
- 可篩選的平台只記錄擁有者實際使用的平台；IGDB 的支援平台僅供參考，不自動加入篩選欄位。
- 電子遊戲的平台選填且可稍後補充；桌遊不顯示平台欄位。
- 平台選擇器含常用預設值並允許新增自訂平台；名稱忽略前後空白及英文字母大小寫以避免重複。
- 預設平台不可修改；仍在使用的自訂平台或自由標籤不得刪除，清空關係後才能確認刪除。
- 相同的「供應商＋來源 ID」在一般與資源回收項目中都只能存在一次；重複建立時開啟既有條目或提示還原。
- 手動建立時以名稱偵測並提示可能重複項目，但不硬性阻止建立；資源回收項目可直接還原。
- 遊戲類型、平台、來源分類與自由標籤是獨立且可篩選的維度。
- 自由標籤是全庫共用的分類項目；改名會套用至所有關聯遊戲，名稱須經去空白及英文大小寫正規化以避免重複。
- 來源分類可隨中繼資料更新，自由標籤不得被外部來源修改。
- 來源分類保留供應商、分類種類、來源 ID 與名稱，並在介面依來源種類分組顯示。
- 來源分類種類只依 API 明確欄位的固定白名單判定，不從文字猜測；IGDB 關鍵字與彙整標籤不納入 MVP 篩選。
- 同一來源分類種類內取聯集，不同種類間取交集。
- 來源分類篩選只在選定單一遊戲類型後顯示；自由標籤可跨遊戲類型篩選。
- 來源分類可保留來源原文；MVP 不自動翻譯外部中繼資料。
- 設計／開發、美術與發行是 MVP 唯一的三個貢獻分類；電子遊戲的個人名單若來源缺漏，允許後續補充，但不能把公司負責人自動視為遊戲設計者。
- 貢獻者只依相同供應商與來源 ID 自動去重；同名僅提示可能重複，MVP 不提供合併工具。
- 來源貢獻關係唯讀，手動貢獻關係可新增及移除；重新整理來源不得影響手動關係。
- MVP 不匯入桌遊建議年齡。
- 桌遊與電子遊戲共用同地多人範圍、單人模式及選填人數說明；純線上互動不納入，結構化人數在 MVP 由來源帶入且唯讀。
- 重度（BGG Avg. Game Weight）是主要動態指標，Strategy Game Rank 是策略桌遊排名；兩者保存最新快照及最後成功更新時間。
- 每個台北日第一次登入後背景更新過期的 BGG 指標，同日最多啟動一次；一般瀏覽不即時抓取，MVP 不保存指標歷史。
- 桌遊卡片顯示重度，並支援重度範圍篩選、重度雙向排序及 Strategy Game Rank 排序。
- 每日背景流程只更新一般收藏庫內 BGG 遊戲的重度與策略排名；完整中繼資料只在手動重新整理時更新。
- 選單顯示背景更新進度、最後成功時間與失敗筆數，並可只重試失敗項目。
- 自動中繼資料範圍以本文件清單為準；不匯入一般評分、整體排名、熱門度、來源影音或純線上人數。
- 來源介紹唯讀、安全清理並預設收合；個人補充使用筆記，手動條目不要求填寫介紹。
- 貢獻者以人物／組織及帶固定分類的關係保存；可點擊查看及依分類篩選收藏庫內遊戲。
- 依貢獻者探索收藏庫外遊戲延至資料來源或索引方案完成後，不屬於 MVP。
- 同一篩選維度內取聯集，不同維度之間取交集；標題搜尋可與篩選並用。
- 以「搜尋、選擇、自動帶入資料」建立遊戲條目。
- 新增時先選遊戲類型；桌遊只搜尋 BoardGameGeek，電子遊戲只搜尋 IGDB，不混合來源結果。
- 來源建立的類型由供應商固定；手動建立送出前再次確認類型，建立後不提供跨類型修改或資料搬移。
- 外部資料找不到或無法使用時，可只填名稱建立，日後再連結來源補齊資料。
- 手動最小條目第一次連結來源屬於 MVP；已連結條目的來源更換不屬於 MVP。
- 手動條目連結來源若命中既有來源身分，必須阻止連結且不得自動合併資料；原手動條目保持不變。
- 標題搜尋同時涵蓋顯示名稱、原文名稱與其他別名。
- MVP 標題搜尋採即時部分字串比對，英文不分大小寫；不承諾錯字推測。
- 收藏庫搜尋只查本地資料；BoardGameGeek 與 IGDB 搜尋只在新增遊戲流程中執行。
- 外部結果須先顯示關鍵資料並由擁有者確認；確認送出時再次檢查來源身分，不能點選即建立。
- 建立成功後進入遊戲詳細頁，手機主要操作為上傳照片與新增筆記，且不自動開啟筆記。
- 桌遊以 BoardGameGeek、電子遊戲以 IGDB 作為主要中繼資料來源。
- 一般中繼資料只在建立條目及手動重新整理時抓取；每日背景同步只更新 BGG 重度與 Strategy Game Rank。
- 重新整理中繼資料不得覆寫擁有者建立或修改的內容。
- 匯入的中繼資料與來源封面永久保存在應用程式管理的持久儲存中；外部服務失效不得影響既有資料的查看與編輯。
- 使用者可在遊戲條目上新增多則帶時間的獨立筆記。
- 筆記以 Markdown 純文字保存並在閱讀時安全渲染；不得直接執行筆記中的原始 HTML。
- MVP 至少交付 Markdown 原始碼編輯器與語法工具列；Vditor `ir` 原型只有在通過行動版驗證時才取代它，且不得阻塞主線。
- 筆記新增與編輯自動儲存，且必須顯示儲存中、成功或失敗狀態；失敗不得遺失目前文字。
- 空白的新筆記不建立；清空既有筆記時暫停自動儲存，只有在應用程式內離開並確認後才移除，意外關閉則保留伺服器舊值。
- 筆記移除一律軟刪除並提供立即復原；MVP 不永久清除筆記，也不建立獨立筆記資源回收頁。
- 自動封面可由自訂圖片覆寫，且可恢復成來源封面。
- 相簿圖片可附選填說明，但說明不得成為上傳的必要步驟。
- 每個遊戲只有一個依上傳時間排列的相簿，不做子相簿或手動排序。
- 遊戲附件支援上傳、描述、開啟與下載，不做文件版本管理或閱讀功能。
- 所有封面、相簿圖片與遊戲附件的單檔上限為 50 MB，且前端與伺服器端都必須驗證。
- 相簿圖片與自訂封面保存原檔，另產生可重建的顯示用縮圖；一般瀏覽優先載入縮圖以節省頻寬。
- 每張圖片只預先產生一份 WebP 縮圖，不使用 Supabase 即時圖片轉換；縮圖失敗不得造成原檔上傳失敗。
- 列表、搜尋與相簿網格只載入縮圖；點開或下載單張圖片時才載入原檔。
- 外部連結可由系統或擁有者建立；重新整理資料不得影響自訂連結。
- MVP 支援一般命名清單與每款遊戲的關聯遊戲清單；項目可引用庫內條目或庫外來源遊戲。
- 關聯遊戲採對稱且無方向的單一關係；具方向性的語意使用一般命名清單。
- 清單項目依遊戲名稱排列，不做手動排序；項目可附建立後再填的選填短說明。
- 遊戲移入資源回收區不移除清單或關聯關係；清單顯示已刪除標記並可直接還原。
- 庫外清單引用不會建立遊戲條目；日後建立相同來源遊戲時須自動接回既有清單關係。
- 庫外清單引用只保存來源身分、名稱、年份與本地 WebP 封面縮圖；正式建立遊戲條目後才保存來源封面原檔。
- 庫外清單引用只能從有穩定來源身分的搜尋結果建立；沒有來源結果時先建立手動遊戲條目。
- 一般清單刪除採封存；不做全域封存頁，而在成員遊戲頁的「相關封存清單」區段提供還原。
- 一般與封存清單名稱經正規化後必須唯一；命中封存清單時引導至成員遊戲頁還原，只有庫外引用時則在建立畫面直接還原。
- 新清單在加入第一個成員前只是未持久化草稿；離開空草稿時提示放棄，無法提示時不留下資料。
- 移除清單成員或解除關聯遊戲皆採軟刪除並提供立即復原。
- MVP 的遊戲條目刪除只移入可還原的資源回收區；遊戲條目不提供永久刪除，也不自動清空。
- 遊戲移入資源回收區前顯示受影響內容數量，完成後可立即復原；資料與 Storage 檔案保持不變。
- MVP 只供單一擁有者使用。
- 外部帳號與收藏庫自動同步不屬於 MVP。
- 多人帳號、共享收藏與分級權限不屬於 MVP。
- 逐場統計、玩家勝負與遊玩歷史不屬於 MVP。

## 存取邊界

- MVP 唯一正式部署目標為 Vercel；QNAP 不參與線上請求。
- 正式環境使用自訂網域並由 Cloudflare Access 管理單一擁有者的登入。
- 應用程式仍須在伺服器端驗證 Cloudflare Access JWT；沒有有效 JWT 的請求一律拒絕，避免從自動產生的 `.vercel.app` 網址繞過保護。
- Vercel 預覽部署另以 Vercel Deployment Protection 保護，不把預覽網址視為公開分享功能。
- 多人帳號、公開註冊及應用程式內帳號管理不屬於 MVP。
- Supabase Auth 不加入 MVP；Cloudflare Access 是唯一登入與人員准入層。

## 部署與審查

- 每次分支或合併請求可建立 Vercel 預覽部署，供桌面與手機瀏覽器審查。
- 日常開發仍可在 Mac 執行本機開發伺服器；正式發行以 Vercel 部署結果為準。
- QNAP Docker 映像與 QNAP 正式部署不屬於 MVP 驗收範圍。
- 線上服務使用 Supabase PostgreSQL 與私有 Supabase Storage；不得依賴 Vercel 執行環境的本機檔案系統保存資料。
- 本機開發使用 Supabase CLI、版本化 migration 與可重建的 bucket／RLS 設定。
- Supabase Realtime 與 Edge Functions 不屬於 MVP。
- 正式環境與預覽環境使用不同的 Supabase 專案與憑證；預覽部署永遠不得取得正式環境 secret 或連線字串。
- 所有 Vercel 預覽部署共用一個固定的 Supabase 測試專案。測試專案只放假資料，可定期重設並重新執行 migration 與 seed。
- 單人 MVP 不為每個分支建立獨立 Supabase 專案或資料庫分支；若未來出現並行測試互相干擾，再重新評估。

## 可復原性與延後範圍

- 正式資料存放於 Supabase PostgreSQL；封面、相簿圖片與遊戲附件存放於私有 Supabase Storage。
- 自動備份、備份保留政策與還原流程不屬於 MVP；在這些功能完成前，系統沒有 QNAP 異地復原保障。
- QNAP 備份列為 MVP 完成後的最高優先項目，但不是 MVP 上線的驗收條件。
- 永久刪除須等 QNAP 備份及還原驗證完成後才能加入；MVP 期間移入資源回收區的資料與 Storage 原檔都持續保留。
- 後續備份須把 PostgreSQL 匯出為可攜格式，並把 Storage 物件連同原始檔名、相對路徑、雜湊與關聯清單下載到 QNAP 的一般資料夾。
- 後續完整備份必須同時涵蓋資料庫匯出與全部 Storage 物件；缺少任一者都視為不完整備份。
- 後續還原驗收必須實際建立新 Supabase 專案、還原資料庫與回傳 Storage 物件，不能只確認備份排程或檔案存在。

## 使用介面

- MVP 提供繁體中文響應式網頁，可在桌面與手機瀏覽器使用。
- 收藏庫首頁預設採響應式封面網格並依遊戲顯示名稱排序。卡片顯示封面、名稱與遊戲類型；電子遊戲另顯示擁有者實際使用的平台，桌遊另顯示重度。
- 收藏庫可切換最近新增、重度與 Strategy Game Rank 等已定義排序；缺少所選排序值的項目排在有值者之後。
- 某次搜尋與篩選條件不永久保留到下次開啟收藏庫。
- 手機端必須能搜尋及新增遊戲、拍照或選取相片上傳，並撰寫筆記。
- 手機端圖片上傳支援一次選取多張，且不要求逐張填寫說明後才能完成。
- 批次圖片逐檔成功或失敗；成功項目保留，失敗項目可個別重試，重試不得複製已成功項目。
- 原生 iOS／Android 應用程式不屬於 MVP。
- MVP 不提供離線模式或 PWA 安裝；讀取與寫入都需要網路連線。
- 斷線或請求失敗時，介面須保留當前尚未完成的畫面內容、明確顯示未完成狀態並允許重試，但不建立離線寫入佇列或恢復連線後的背景同步。
